import React from 'react'

const DateCards = ({name}) => {
  return (
    <div>
        <br/>
        <span>{name} </span>
        <button>Setup a Date</button>
        <br/>
    </div>
  )
}

export default DateCards